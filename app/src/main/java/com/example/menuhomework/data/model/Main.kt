package com.example.menuhomework.data.model

import kotlin.random.Random

class Main {
    var temp = Random.nextFloat()
    var pressure = Random.nextInt(0, 60)
    var humidity = Random.nextInt(0, 60)
}
